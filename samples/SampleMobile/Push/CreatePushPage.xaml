<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:push="clr-namespace:SampleMobile.Push" 
             xmlns:sv="clr-namespace:AiForms.Renderers;assembly=SettingsView"
             x:DataType="push:CreatePushViewModel"
             x:Class="SampleMobile.Push.CreatePushPage"
             Title="Push">
    
    <sv:SettingsView>
        <sv:Section Title="Notification">
            <sv:EntryCell Title="Title" ValueText="{Binding NotificationTitle}" />
            <sv:EntryCell Title="Message" ValueText="{Binding NotificationMessage}" />
        </sv:Section>
        
        <sv:Section Title="Filters">
            <sv:ButtonCell Command="{Binding SetDeviceTokenToMe}" Title="Set To My Token" />
            <sv:EntryCell Title="Device Token" ValueText="{Binding DeviceToken}" />
            <sv:EntryCell Title="User ID" ValueText="{Binding UserId}" />
            <sv:EntryCell Title="Tags (comma separated)" ValueText="{Binding Tags}" />

            <sv:CheckboxCell Title="Send to iOS" Checked="{Binding SendToIos}" />
            <sv:CheckboxCell Title="Send to Android" Checked="{Binding SendToAndroid}" />
        </sv:Section>
        
        <sv:Section>
            <sv:CommandCell Command="{Binding Register}" Title="Register" />
            <sv:CommandCell Command="{Binding UnRegister}" Title="Un-Register" />
            <sv:CommandCell Command="{Binding Send}" Title="Send" />
        </sv:Section>
    </sv:SettingsView>
</ContentPage>