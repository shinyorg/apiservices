<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sv="clr-namespace:AiForms.Renderers;assembly=SettingsView"
             xmlns:mail="clr-namespace:SampleMobile.Mail"
             x:DataType="mail:CreateMailViewModel"
             x:Class="SampleMobile.Mail.CreateMailPage"
             Title="Send Mail">

    <ContentPage.Content>
        <sv:SettingsView>
            <sv:Section>
                <sv:EntryCell Title="Template Name" ValueText="{Binding TemplateName}" />
                <sv:CheckboxCell Title="Is High Priority" Checked="{Binding IsHighPriority}" />
                <sv:EntryCell Title="TO Address" ValueText="{Binding ToAddress}" />
                <sv:EntryCell Title="Reply-To Address" ValueText="{Binding ReplyToAddress}" />
                <sv:EntryCell Title="Subject" ValueText="{Binding Subject}" />
                <sv:EntryCell Title="Body Message" ValueText="{Binding Body}" />
            </sv:Section>

            <sv:Section>
                <sv:CommandCell Title="Test Parse" Command="{Binding Parse}" />
                <sv:CommandCell Title="Send" Command="{Binding Send}" />
            </sv:Section>
        </sv:SettingsView>
    </ContentPage.Content>
</ContentPage>